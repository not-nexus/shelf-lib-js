"use strict";

module.exports = () => {
    class URI {
        /**
         * @param {string} host
         */
        constructor(host) {
            this.host = host;
        }
    }

    return URI;
};

